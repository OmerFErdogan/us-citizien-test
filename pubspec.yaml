name: us_citizenship_test
description: "US Citizenship Test - Prepare for your US citizenship exam with official questions"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.3
  fl_chart: ^0.64.0
  auto_size_text: ^3.0.0
  # google_mobile_ads: ^6.0.0  # Temporarily disabled
  intl_translation: ^0.20.1
  flutter_svg: ^2.0.10
  lottie: ^3.1.0
  purchases_flutter: ^8.8.1
  flutter_animate: ^4.2.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/questions.json
    - assets/images/
    - assets/images/badges/
    - assets/images/onboarding/
